rules:
  - id: no-md5-usage
    patterns:
      - pattern: md5.Sum(...)
    message: "MD5 is cryptographically broken. Use SHA-256 or bcrypt for password hashing."
    languages: [go]
    severity: ERROR