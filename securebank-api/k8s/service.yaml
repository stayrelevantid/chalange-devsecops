apiVersion: v1
kind: Service
metadata:
  name: securebank-api
  namespace: securebank
spec:
  selector:
    app: securebank-api
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP